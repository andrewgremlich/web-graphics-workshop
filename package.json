{
  "name": "web-graphics-workshop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "pnpm build && wrangler deploy --config dist/wrangler.json",
    "check": "pnpx @biomejs/biome check --write ./src"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.47.0",
    "@types/node": "^25.9.5",
    "typescript": "~7.0.2",
    "vite": "^8.1.5",
    "wrangler": "^4.114.0"
  },
  "dependencies": {
    "lucide": "^1.27.0"
  }
}
